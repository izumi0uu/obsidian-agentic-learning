---
type: map
topic:
  - metadata
  - obsidian
status: active
created: 2026-05-05
updated: 2026-05-10
related:
  - "[[插件配置]]"
  - "[[LLM Wiki 工作流]]"
  - "[[04 页面目录]]"
---

# 字段规范

这页定义 vault 的基础字段。以后新增笔记优先遵守这里，而不是临时发明字段。

## 核心字段

```yaml
---
type:
topic:
status:
created:
updated:
source:
evidence:
last_checked:
freshness:
conflicts:
related:
---
```

## type

`type` 说明这篇笔记是什么。

- `source`：外部资料、网页、论文、视频。
- `concept`：概念卡。
- `map`：导航页、主题地图、路线图。
- `experiment`：实验记录。
- `frontier`：前沿追踪条目。
- `project-index`：项目或实验集合索引。
- `index`：vault 入口页。

## source_type

`source_type` 只用于 `type: source` 的 raw 笔记。

- `paper`：论文。
- `docs`：官方文档。
- `repo`：GitHub 项目、示例代码、SDK 仓库。
- `article`：文章、报告、实践指南。
- `web`：一般网页剪藏。
- `video`：视频、课程。

## topic

`topic` 说明这篇笔记属于什么主题。优先用小写英文，方便 Dataview 查询。

常用值：

- `agent`
- `llm`
- `rag`
- `memory`
- `evaluation`
- `obsidian`
- `workflow`
- `frontier`

## status

`status` 说明处理状态。

- `inbox`：刚收集，还没处理。
- `seed`：已经有初步理解。
- `growing`：正在补例子、边界和链接。
- `mature`：相对稳定，可以复习或引用。
- `review`：需要重新检查或更新。
- `active`：导航页或长期活跃页面。

## source

`source` 只放来源链接或来源笔记。

概念卡里的 `source` 不等于参考文献列表。它回答：“这张卡最初从哪里长出来？”

## evidence

`evidence` 放更细的证据锚点。它回答：“这张卡里的关键理解，最少可以回到哪几个 source 小节或原文位置？”

优先格式：

```yaml
evidence:
  - "Source Note#为什么收"
  - "Source Note#需要我读的内容"
  - "PDF p.3 / Section 2.1"
```

边界：`source` 是来源列表，`evidence` 是最小证据锚点。没有精读到页码时，先用 source note 的小节锚点，不要假装有段落级引用。

## last_checked / freshness

`last_checked` 用于会更新的来源和前沿概念，格式用 `YYYY-MM-DD`。

`freshness` 用于判断下次是否要复查：

- `stable`：论文、基础概念、历史资料，不需要频繁查新。
- `watch`：前沿方向、框架、repo、协议，建议每月复查。
- `volatile`：API、产品文档、安全榜单、快速变化项目，建议每 1-2 周复查。
- `stale`：已经发现可能过期，优先复查。

## conflicts

`conflicts` 记录潜在矛盾或待核对说法。

```yaml
conflicts:
  - "OpenAI 和 Anthropic 对 Agent 定义边界不同，待比较。"
```

没有矛盾时可以留空数组或省略。发现矛盾后，不要直接覆盖旧说法；先写入 [[06 Wiki 健康检查]] 或 [[02 问题池]]。

## related

`related` 放最重要的 2-6 个相关双链。

不要把所有相关页面都塞进去。太多 related 会让地图变成噪音。

## 边界规则

- 目录决定文件物理位置。
- `type` 决定这篇笔记的角色。
- `topic` 决定它属于哪条知识线。
- `status` 决定下一步要不要整理。
- `related` 决定它在图谱里怎么连接。
- `evidence` 决定这张卡是否能追溯到来源。
- `freshness` 决定它是否需要定期复查。
